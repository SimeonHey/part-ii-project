plugins {
    id 'java'
}

dependencies {
    testImplementation 'junit:junit:4.12'
    implementation group: 'org.apache.kafka', name: 'kafka_2.12', version: '2.3.1'
    testImplementation group: 'org.mockito', name: 'mockito-core', version: '3.1.0'
    testImplementation group: 'com.google.code.gson', name: 'gson', version: '2.8.6'

    implementation project(':simple_olep:common')
    implementation project(':simple_olep:storage_systems:lucene')
    implementation project(':simple_olep:storage_systems:psql')
}

test {
    useJUnit()
    maxHeapSize = '1G'
}

